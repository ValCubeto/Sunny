fun main(args: list) -> void:
	print(args.size)


/* fun main(args: str[*]) -> void:
	print({ args }) */
	// entries.push([key, scope[key]])

type Not<...T: Type[]> = Type((value, model as { T }) => xtype(value) not in T)
