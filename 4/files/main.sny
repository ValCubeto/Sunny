# comment
"Hola\sexo"
'char = \u{42}' # \r\u{41} = A
123 # a number

# fun main(args: str[*])
fun main(args: list<str, range(1, *)>) -> none {
	println('Hello world')
}

typedef Lit<...values: list> = Type::new((value) => value in values)

class Type {
	static fun new(test: (value) -> bool) {
		class _ { test }
		return _
	}
}

