# = 'hola'

fun main() {
	println('Hello world!')
	for i in 0 to 10 {
		println({ i })
	}
}


if logged_in {
	println({ user })
	# {
	# 	user := User {
	# 		name := 'Jhon'
	# 		age := 30
	# 	}
	# }
} else if age >= 18 {
	println('allowed')
}

try {
	list(0 to 10)
	(0 to 10).to_list()
} # <$(typeof(src) != 'object' ? typeof(src) : src.<proto>.constructor.name)>.$(prop_name) is not a function

infinity
-infinity
none

