# comment

fun create<T>(p: T) -> T
where
  T: Init
{
  return T::new()
}

struct Point {
  pub (x: u8, y: i32) = 0
}

fun Point::new(x = 0, y = 0) {
  return Point { x, y }
}

fun main(_args: String[]) {
  create<Point>().display()
}