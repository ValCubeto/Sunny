# comment

trait New<T> {
	fun new(*) -> (T | Result<T, _>)
}

fun create<T>(p: T = T::new()) -> T
where
	T: New<T>
{
	return p
}

struct Point {
	x: u8
	y: u8
}

fun Point::new(x = 0_u8, y = 0_u8) {
	return Point { x, y }
}

fun main(_args: String[]) {
	create<Point>()
}