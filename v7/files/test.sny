# comment

trait Init {
  fn new()
}

fun create<T>(p: T) -> T
where
  T: Init
{
  return T::new()
}

struct Point {
  pub [x, y]: u8 = [0, 0]
}

fun Point::new(x, y) {
  return Point { x, y }
}

extend Point with Display

fun main(_args: String[]) {
  create<Point>().display()
}