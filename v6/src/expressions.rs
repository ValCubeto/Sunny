pub struct Expression {
	items: Vec<Key>
}

pub fn parse_expr(ctx: &mut Context) -> Expression {
	ctx.ignore_spaces();
	let mut expression = Expression {
		items: Vec::new()
	};

	while ctx.idx < ctx.
	match ctx.ch {}

	expression
}